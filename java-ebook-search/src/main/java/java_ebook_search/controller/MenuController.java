package java_ebook_search.controller;

/**
 * The MenuController class handles the GUI events, calling the methods that will
 * interact with Lucene.
 *
 * @author Aidan O'Grady
 * @since 0.1
 */
public class MenuController {

    /**
     * Quits the system.
     */
    public void exit() {
        System.exit(0);
    }
}
